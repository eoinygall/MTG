import React from 'react';
import { Link } from 'react-router-dom';

function NavBar() {
  return (
    <div className="navbar">
      <input type="text" placeholder="Search..." />
      {/* Add additional sort buttons or features as needed */}
    </div>
  );
}

export default NavBar;
