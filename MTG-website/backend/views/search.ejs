
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link rel="stylesheet" href="MTG-website\src\styles.css"> <!-- Update with the correct path -->
</head>
<body>

    <div class="container">
        <h1>Search Results for "<%= query %>"</h1>

        <% if (cards.length > 0) { %>
            <ul>
                <% cards.forEach(card => { %>
                    <li><strong><%= card.name %></strong> - <%= card.type %></li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No results found for "<%= query %>".</p>
        <% } %>
    </div>
</body>
</html>
